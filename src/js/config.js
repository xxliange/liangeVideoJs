/**
 * 配置信息
 */

const config = {
    width:null,
    height:null,
    // 默认控制器配置
    controls : [
        'progressBar', //进度条
        'playCon', //播放暂停按钮
        'voice', // 声音控制
        // 'setting', //设置
        // 'playMode', //播放方式
        'timeText', //时间显示
        'loop', //循环
        'speed', //播放速度设置
        'fullScreen', //全屏
    ],
    url:null,
    title:null
};

export default config;